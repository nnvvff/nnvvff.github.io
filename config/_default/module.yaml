# This homepage uses a local Hugo layout and does not need a remote theme.
imports: []
mounts:
  - source: layouts
    target: layouts
  - source: content
    target: content
    includeFiles:
      - '_index.md'
